require 'fileutils'

default_platform(:ios)

platform :ios do
  desc "Description of what the lane does"
  lane :update do |options|

    tagNum = options[:version]
    ruby_learn(tag:tagNum)
  end
end
